<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
      xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">

	<item android:id="@+id/action_permissions"
	      android:title="@string/action_permissions_caption"
	      app:showAsAction="ifRoom"/>

	<item android:id="@+id/action_watchers"
	      android:title="@string/action_watchers_caption"
	      app:showAsAction="ifRoom"/>

	<item android:id="@+id/action_settings"
	      android:title="@string/action_settings_caption"
	      app:showAsAction="never"/>
</menu>
